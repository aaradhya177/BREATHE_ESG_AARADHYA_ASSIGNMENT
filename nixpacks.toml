[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["cd backend && python3 -m pip install -r requirements.txt"]

[start]
cmd = "cd backend && python3 manage.py migrate --noinput && gunicorn breathe.wsgi:application --bind 0.0.0.0:$PORT"s